pipeline {
  agent any
   
  stages {
     
      
    stage('Destroy EC2') {
      steps {
          withAWS(credentials: 'aws-credentials', region: 'us-west-1') {
            sh '''
               terraform init
               terraform destroy -auto-approve
               '''
       }
     }
   } 
  }
}
